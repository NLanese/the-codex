import React, { useEffect, useState } from 'react';
import PropTypes from 'prop-types';

export const OstrichToggleButton = ({
    style,
    inactiveStyle=false,
    title,
    caption=false,
    activeColor,
    inactiveColor,
    titleStyle,
    captionStyle,

    isActive=true,
    onSubmit,
}) => {

}
